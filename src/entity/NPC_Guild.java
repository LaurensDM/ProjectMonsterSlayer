package entity;

import gui.GamePanel;

public class NPC_Guild extends Entity{
    /**
     * Instantiates a new Entity.
     *
     * @param gp the gp
     */
    public NPC_Guild(GamePanel gp) {
        super(gp);
    }
}
