package entity;

import gui.GamePanel;

public class NPC_Random extends Entity{
    /**
     * Instantiates a new Entity.
     *
     * @param gp the gp
     */
    public NPC_Random(GamePanel gp) {
        super(gp);
    }
}
